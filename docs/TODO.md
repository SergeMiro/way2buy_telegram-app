# TODO — що лишилось зробити

Оновлено: 18.08.2026. Живий документ: закрите — викреслювати, нове — дописувати.

Позначки: **[Сергій]** — без вас не рухається · **[код]** — робота в репозиторії ·
**[рішення]** — треба продуктовий вибір, не технічний.

---

## 1. Бренд картки: три джерела за пріоритетом

Правило (реалізоване 18.08.2026):

| # | Джерело | Де в коді | Стан |
|---|---------|-----------|------|
| 1 | **Підпис поста** | `parsePostText()` → `detectBrand()` у `server/telegram.js` | ✅ працює |
| 2 | **Назва каналу** | той самий `parsePostText()`, друга спроба | ✅ працює |
| 3 | **Фотографія (Gemini)** | `server/vision.js` | ⚠️ написано, **не увімкнено** — немає ключа |

Звідки взявся бренд, видно в колонці `posts.brand_source`:
`text` · `channel` · `vision` · `vision-none` (модель дивилась і нічого не впізнала) ·
`NULL` (черга — ще ніхто не відповів).

**Підпис ніколи не переглядається.** Пост 71627 у `@w2b_luxury_bags` — сумка Miu Miu
з підписом «Gucci». Картка каже Gucci і казатиме далі: виправляти канал — справа
продавця, застосунок лише заповнює **мовчання**.

### Що вже дало результат

Черга «бренду немає» була **1867** карток. Виявилось, що майже всі вони бренд
**називають** — просто так, що матчер його не бачив:

* стилізований юнікод Telegram: `𝗟𝗢𝗨𝗜𝗦 𝗩𝗨𝗜𝗧𝗧𝗢N`, `𝐂𝐚𝐫𝐭𝐢𝐞𝐫`, `𝒁𝑰𝑴𝑴𝑬𝑹𝑴𝑨𝑵𝑵`;
* кириличні двійники латиниці: `Нermes` (кирилична Н), `Chanеl`, `Valentinо`,
  `BERLUTІ`, `Jimmy Choо`. Схоже, навмисно — підпис, який не пише торгову марку
  буквально, не ловлять фільтри;
* доми, яких у списку просто не було: The Row, Zegna, Berluti, Kiton, Omega,
  Patek Philippe, Alaïa, Aquazzura, Zimmermann, Balmain, Messika, Graff та інші.

Після нормалізації (`foldForMatch()`) і розширення словника до 76 домів:

```
було:  1867 у черзі
стало:  345 у черзі   (1522 закрито з підпису, 136 неправильних виправлено)
```

Тобто **фотографію треба дивитись у 345 карток, а не в 1867** — і ціна питання
впала приблизно в п'ять разів ще до першого виклику моделі.

### 1.1 ✅ Ключі є, моделі підключені (18.08.2026)

Замість Gemini — **OpenRouter + OpenCode Zen, усі моделі безкоштовні**. Ланцюги
живуть у `server/llm.js`; це копія списків `vision` та `analytics` з llmfp на
цьому VPS. Сам llmfp у справі не бере участі: він слухає `127.0.0.1`, а
застосунок працює на Vercel.

* зір → `server/vision.js`, ланцюг `vision`, 5 моделей;
* аналітика → `server/ai.js` (звіт), ланцюг `analytics`, 14 моделей.

⚠️ **У безкоштовного OpenCode Zen зору немає взагалі**: на запит із картинкою
моделі віддають `404 No endpoints found`, тоді як на текст та сама модель
відповідає. Тому ланцюг зору — з одного провайдера, і це його єдина точка
відмови. Gemini, коли дійдуть руки, її і закриє.

Перевірено на справжніх фото: з 12 карток модель правильно назвала 10.

### 1.2 [код] Прогнати чергу — після заповнення фотографій (§5)

```bash
npm run brands -- --dry          # подивитись, що саме в черзі, без викликів
npm run brands -- --limit 25     # спершу 25 і перевірити відповіді очима
npm run brands                   # решта, з паузами
```

⚠️ Спершу має відпрацювати `npm run photos` (§5): усі 327 карток у черзі
зберігали мертві посилання, і моделі не було на що дивитись. Щойно обкладинки
осядуть у сховищі — черга розбирається сама, планувальником, по 8 за годину.

Планувальник (`server/scheduler.js`) і сам бере по 8 карток на тик щогодини, тож
нові пости підбиратимуться самі. Разовий скрипт потрібен лише щоб зняти залишок
одразу, а не за два тижні.

### 1.3 [код] Черга не видна в кабінеті

У «Картки з каналів» показуються перші 20 карток і кнопка «Змінити» — виправити
бренд можна, **знайти** потрібну картку не можна. Треба:

* фільтр «без бренду» / «бренд з фото» у списку;
* показувати `brand_source` біля картки, щоб було видно, чиє це слово;
* лічильник черги десь на видноті.

### 1.4 [код] Словник домів доростатиме

Магазин заводить нові марки, і кожна нова — це знову картки без бренду. Раз на
місяць варто дивитись, що накопичилось у черзі:

```sql
SELECT trim(regexp_replace(body, '(?i)(size|розмір|размер)[^\n]*', '', 'g')) AS caption,
       count(*) cards
FROM posts WHERE brand_source IS NULL AND status='published'
GROUP BY 1 ORDER BY cards DESC LIMIT 40;
```

Що бачимо там регулярно — дописуємо в `BRANDS` у `server/telegram.js`.

---

## 2. Знайдене принагідно і вже виправлене (для протоколу)

* **`/api/admin/report` віддавав 500 місяцями.** SQL, написаний під SQLite:
  `HAVING n<=1` за аліасом і `MAX(created_at).slice(0,10)` на об'єкті `Date`.
  Плюс у коді стояв заморожений годинник `new Date('2026-07-21…')` — «звіт за
  сьогодні» вічно звітував про один липневий вечір. Тестів на модуль не було
  взагалі; тепер є (`tests/report.test.js`).
* **Фасети каталогу виконувались 376 мс.** Карта видимості Postgres була
  протухла після імпорту ~2000 постів, і index-only scan ходив у купу за кожним
  рядком. Після `VACUUM (ANALYZE)` — 2.2 мс. Синк тепер вакуумить за собою.
* **Редагування поста в каналі не оновлювало бренд.** Оновлювались заголовок і
  текст, а бренд лишався старий — звідси картки «Gucci · сумка» під фільтром
  Saint Laurent (136 штук). Там же виявилось, що це оновлення не поважало
  `curated`: правка з кабінету мовчки затиралась редагуванням у каналі.
* **Пересинк стирав би бренд, знайдений по фото** — `excluded.brand` для таких
  карток порожній. Тепер вердикт моделі переживає синхронізацію (і `vision-none`
  теж, щоб не платити за те саме фото двічі).

---

## 3. Решта хвостів

### 3.1 [код] Повідомлення від бота — тільки українською

Інтерфейс говорить трьома мовами (укр/рос/англ, вибір у топбарі), а все, що
надсилає сервер — привітання з днем народження, нагадування про примірочну,
промокоди — жорстко українською. Клієнт, який відкрив застосунок російською,
отримує DM українською.

Треба: зберігати обрану мову в `customers`, і слати DM нею.

### 3.2 [код] Ціна з поста-порівняння

У стрічці трапляється картка з ціною «$1 100», зібраною з поста, де порівнюються
дві речі. Той самий клас, що й бренди: у підписі кілька чисел, парсер бере не те.

### 3.3 [рішення] Картки без жодного підпису

12 карток у черзі не мають підпису взагалі — там і читати нічого. Для них фото
єдине джерело; якщо і модель не впізнає, картка лишиться без бренду і не
потрапить у жоден фільтр. Питання: показувати такі у вітрині чи ховати?

### 3.4 [код] Колаборації

`Miumiu x New Balance` — дві марки в підписі. Зараз перемагає та, що вища в
списку (Miu Miu). Для колаборації це прийнятно, але правило випадкове: воно
залежить від порядку рядків у файлі, а не від порядку слів у підписі.

---

## 4. Що треба від Сергія — коротко

| Що | Для чого | Стан |
|---|---|---|
| ~~`GEMINI_API_KEY`~~ | замінено на OpenRouter + OpenCode | ✅ зроблено |
| ~~рішення по §5~~ | обрано Supabase Storage | ✅ зроблено |
| рішення по 3.3 | картки без підпису — ховати чи лишати | ⏳ чекаю |


---

## 5. ✅ Фотографії: байти тепер наші (рішення Сергія, 18.08.2026)

Знайдено під час підключення зору, полагоджено того ж дня.

**Рішення:** зберігати байти в **Supabase Storage**, бакет `photos`, публічний.
`server/photos.js` копіює обкладинку перед записом рядка, і в базу лягає вже
постійне посилання — решта застосунку не змінилась, бо `mediaUrl()` і так
пропускає URL як є.

**Розмір, бо план на 1 ГБ.** 55 848 фото по 156 КБ — це 8.7 ГБ, тому
зберігається **тільки обкладинка**, стиснута до 720px q76 ≈ 77 КБ. Уся вітрина —
близько **0.46 ГБ**, половина бюджету. `W2B_PHOTO_KEEP` підніме кількість, коли
підніметься план. Стиснення робить `sharp`, він у devDependencies і вантажиться
через `await import` — якщо його немає (Vercel), байти лягають як є.

**Прибирання:** `pruneOutsideWindow` тепер видаляє і об'єкти разом із картками,
інакше бакет ріс би вічно, а каталог — ні.

**Заповнення:** `npm run photos` — глибокий обхід усіх каналів, бо живе
посилання на старий пост можна взяти тільки з живої сторінки. Відновлюваний:
курсор пишеться в рядок каналу після кожного проходу.

### Історія (для протоколу)

**Що відбувається.** Картки, імпортовані з публічної сторінки каналу
(`t.me/s/…`), зберігають посилання виду
`https://cdn4.telesco.pe/file/…`. Ці посилання **тимчасові**. Перевірка:

```
свіже посилання, щойно з t.me/s/   → HTTP 200, image/jpeg
збережене в базі                   → HTTP 404
випадкова вибірка 20 карток        → 20 з 20 мертві
```

**Масштаб.** 6349 з 6730 опублікованих карток зберігають посилання — і жодне з
них уже не відкривається. Працюють лише 381 картка, що прийшли ботом (там
`file_id`, а не посилання, і його резолвить сам Telegram). Свіжі пости в топі
стрічки виглядають нормально саме тому — вони від бота.

**Чому це не помітили.** У застосунку є запасний варіант: не завантажилось фото
— малюється 🛍️. Тобто вітрина не «зламана», вона просто порожня на вигляд.

Обрано варіант «зберігати байти» — див. вище.
